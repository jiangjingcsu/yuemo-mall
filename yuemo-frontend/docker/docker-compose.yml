services:
  frontend-server:
    image: ${CI_REGISTRY:-192.168.1.54:5050}/jiangjingcsu/yuemo-mall/yuemo-mall-frontend:${IMAGE_TAG:-latest}
    container_name: frontend-server
    ports:
      - "8080:80"
    restart: always
    networks:
      - frontend-net

networks:
  frontend-net:
    external: true
