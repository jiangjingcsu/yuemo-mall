services:
  frontend-server:
    image: 192.168.1.53/yuemo-mall/frontend:${IMAGE_TAG:-latest}
    container_name: frontend-server
    ports:
      - "8080:80"
    restart: always
    networks:
      - frontend-net

networks:
  frontend-net:
    external: true
